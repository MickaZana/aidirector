[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @aidirector/web build"

[deploy]
startCommand = "pnpm --filter @aidirector/web start"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
