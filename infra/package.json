{
  "name": "@aidirector/infra",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "install": "pulumi install",
    "preview": "pulumi preview --stack prod",
    "up": "pulumi up --stack prod --yes",
    "destroy": "pulumi destroy --stack prod --yes"
  },
  "dependencies": {
    "@pulumi/pulumi": "^3.142.0",
    "@pulumi/cloudflare": "^5.44.0",
    "@pulumi/vercel": "^3.0.0",
    "@pulumi/github": "^6.4.0",
    "@pulumi/random": "^4.17.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
